# comment out to run tests MUCH faster...

# import datawrangler as dw
# import os
# from glob import glob as lsdir
#
# # reset data directory
# data_directory = eval(dw.core.get_default_options()['data']['datadir'])
# for f in lsdir(os.path.join(data_directory, '*.*')):
#     os.remove(f)
